import { uploadFile } from "@/lib/upload";
import { useState } from "react";

export default function DocHome() {
  return <div className="flex flex-col items-center justify-center h-screen">Home Page</div>;
}
