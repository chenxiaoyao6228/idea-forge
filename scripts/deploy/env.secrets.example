# =============================================================================
# SECRETS OVERRIDE FILE
# =============================================================================
# This file contains ONLY the secrets you need to override from .env.example
#
# IMPORTANT:
# - The Docker image already includes .env.example with safe defaults
# - You only need to specify secrets and environment-specific values here
# - All other values are inherited from .env.example in the container
#
# Usage:
#   1. Copy this file: cp env.secrets.example env.secrets
#   2. Fill in your actual secret values below
#   3. Deploy: ./deploy.sh production (or test)
# =============================================================================

# Environment (production or test)
NODE_ENV=production

# Docker Image
# For production: chenxiaoyao6228/idea-forge:latest
# For test: chenxiaoyao6228/idea-forge:test
IMAGE_NAME=chenxiaoyao6228/idea-forge:latest

# =============================================================================
# DATABASE
# =============================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here
POSTGRES_DB=ideaforge

# =============================================================================
# SECRETS (REQUIRED - Generate with: openssl rand -base64 32)
# =============================================================================
SESSION_SECRET=CHANGE_ME_session_secret_here
JWT_SECRET=CHANGE_ME_jwt_secret_here
REFRESH_TOKEN_SECRET=CHANGE_ME_refresh_token_secret_here
COLLAB_SECRET_KEY=CHANGE_ME_collab_secret_here

# =============================================================================
# OAUTH (Get from provider consoles)
# =============================================================================
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=https://yourdomain.com/api/auth/google/callback

GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_CALLBACK_URL=https://yourdomain.com/api/auth/github/callback

# =============================================================================
# EMAIL
# =============================================================================
RESEND_API_KEY=your_resend_api_key
EMAIL_FROM=noreply@yourdomain.com
SUPER_ADMIN_EMAIL=admin@yourdomain.com

# =============================================================================
# STORAGE - Choose ONE: Self-hosted MinIO OR Cloud OSS
# =============================================================================

# --- Option 1: Self-Hosted MinIO  ---
# Uncomment these lines to use self-hosted MinIO:
#
# OSS_PROVIDER=minio
# MINIO_ROOT_USER=minio_admin
# MINIO_ROOT_PASSWORD=change_me_strong_password_min_8_chars
# MINIO_PORT=9000
# MINIO_CONSOLE_PORT=9001
# OSS_SECRET_ID=minio_admin  # Must match MINIO_ROOT_USER
# OSS_SECRET_KEY=change_me_strong_password_min_8_chars  # Must match MINIO_ROOT_PASSWORD
# OSS_BUCKET=idea-forge-storage
# OSS_REGION=us-east-1
# OSS_ENDPOINT=http://minio:9000  # Internal Docker network
# OSS_CDN_ENDPOINT=https://yourdomain.com/storage  # Public URL via Nginx
#
# After deployment: Create bucket via console at https://yourdomain.com/minio-console/

# --- Option 2: Cloud OSS (Aliyun/Tencent/AWS) ---
# Use this for cloud storage providers (default: Tencent COS)
# disable MinIO service, no need to pull minio docker image
ENABLE_MINIO_PROFILE=skip
OSS_PROVIDER=cos # can be any name, just a mark
OSS_SECRET_ID=your_oss_secret_id
OSS_SECRET_KEY=your_oss_secret_key
OSS_BUCKET=your-production-bucket
OSS_REGION=ap-guangzhou
OSS_ENDPOINT=https://your-bucket.cos.ap-guangzhou.myqcloud.com
OSS_CDN_ENDPOINT=https://cdn.yourdomain.com
# Supports: cos (Tencent), oss (Aliyun), s3 (AWS) - adjust endpoint accordingly

# =============================================================================
# AI PROVIDERS
# =============================================================================
DEEPSEEK_API_KEY_1=your_deepseek_api_key

# =============================================================================
# MONITORING
# =============================================================================
SENTRY_DSN=your_sentry_dsn

# =============================================================================
# CLIENT URLS (Must match your domain)
# =============================================================================
CLIENT_APP_URL=https://yourdomain.com
CLIENT_COLLAB_WS_URL=wss://yourdomain.com:5001/collaboration
CLIENT_SENTRY_DSN=your_client_sentry_dsn


# If you want to overwrite more envs, refer to  https://github.com/chenxiaoyao6228/idea-forge/.env.example 