/**
 * Server-side utilities for TipTap editor
 *
 * These utilities can be used in backend services (NestJS API, workers, etc.)
 * without requiring a full editor instance.
 */

export * from "./server/mention-parser";
